import g_cnstnts

# Returns this device's lot ID number
def getLotID():
    # Check for lot #
    return 5

    # Return -1 if no set ID
    return g_cnstnts.INVLD_LOT_ID

# Returns the specified camera's last updated time
def getLastUpdt(id):
    # Check for lot #
    return 5

    # Return -1 if no set ID
    return g_cnstnts.INVLD_LOT_ID